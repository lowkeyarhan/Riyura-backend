# Managed Redis — starts redis-server alongside the app in dev.
# Set REDIS_MANAGED=false in production to use an external Redis instance.
redis:
  managed: ${REDIS_MANAGED:true}

spring:
  application:
    name: Riyura

  # Redis Configuration (Watch Party ephemeral state)
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}

  # Supabase DataSource Configuration
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

    # HikariCP Settings
    hikari:
      maximum-pool-size: 15
      minimum-idle: 2
      idle-timeout: 30000
      max-lifetime: 1800000

  # JPA Settings
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

# TMDB Configuration
tmdb:
  api-key: ${TMDB_API_KEY}
  base-url: ${TMDB_BASE_URL:${TMDB_API_BASE_URL}}
  image-base-url: ${TMDB_IMAGE_BASE_URL}

# Supabase Security Configuration
supabase:
  jwt-secret: ${SUPABASE_JWT_SECRET}

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    groups-order: ASC
    operations-sorter: method
    tags-sorter: alpha
    disable-swagger-default-url: true
  group-configs:
    - group: all
      paths-to-match:
        - /api/**
    - group: explore
      paths-to-match:
        - /api/explore/**
    - group: watchlist
      paths-to-match:
        - /api/watchlist/**
    - group: profile
      paths-to-match:
        - /api/profile/**
    - group: banner
      paths-to-match:
        - /api/banner/**
    - group: movies
      paths-to-match:
        - /api/movies/**
    - group: tv
      paths-to-match:
        - /api/tv/**
    - group: anime
      paths-to-match:
        - /api/anime/**
    - group: party
      paths-to-match:
        - /api/party/**
